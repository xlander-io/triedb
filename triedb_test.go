package triedb

import (
	"fmt"
	"testing"
)

func Test_Cache_Expire(t *testing.T) {

	var x []byte = nil

	fmt.Println(len(x))

	//x := ([32]byte(*n.Child_nodes_hash))
	//result = append(result, x[:]...)

	// result, err := hex.DecodeString("A")
	// fmt.Println(err)
	// fmt.Println(result)
	//hash := newHashFromString("0Xaa0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef")
	//fmt.Printf("%x", hash)
}
